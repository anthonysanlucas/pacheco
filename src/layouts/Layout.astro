---
import '../styles/global.css';
import BackToTopButton from '../components/BackToTopButton.astro';

interface Props {
	title?: string;
	description?: string;
}

const {
	title = 'PACHECO — Comunicación con criterio',
	description = 'Estrategia y acompañamiento para decidir qué decir, cómo decirlo y cuándo conviene no decir nada.'
} = Astro.props;
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<meta name="description" content={description} />
		<meta name="theme-color" content="#fbfaf7" />
		<meta property="og:type" content="website" />
		<meta property="og:locale" content="es_EC" />
		<meta property="og:title" content={title} />
		<meta property="og:description" content={description} />
		{/* Arriba del pliegue el unico texto visible es el CTA del header en
		    Helvetica-Bold (el h1 del Hero es sr-only y el banner es video). Al
		    precargarla resuelve directo, sin pintar antes con el fallback. Si
		    el CTA cambia de cara, hay que mover el preload con el. */}
		<link rel="preload" href="/fonts/Helvetica-Bold.ttf" as="font" type="font/ttf" crossorigin />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body>
		<a class="skip-link" href="#contenido">Saltar al contenido</a>
		<slot />
		<BackToTopButton />
	</body>
</html>
